declare class Sound {
    constructor(src: string);
    //loading: Promise<void> | undefined;
    //volume: number;
    //fade(volume: any, {duration, from, type}): Promise<void>;
    //load({autoplay, autoplayOptions}): Promise<Sound>;
    //play({loop, offset, volume, fade}): void;
}